apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: micro
spec:
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: LoadBalancer
