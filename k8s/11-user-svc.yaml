apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: micro
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
